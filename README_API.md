# Notion/Trello-like Project Management API

Setup:
- npm i
- npm run prisma:generate
- npm run prisma:migrate
- npm run dev

Routes:
- /projects
- /columns
- /steps
- /tags
- /attachments
- /comments

DB schema: prisma/schema.prisma (SQLite by default)
